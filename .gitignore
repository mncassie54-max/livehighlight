# 파이썬 가상환경 — Mac 아키텍처/경로에 묶여 있어 공유하면 깨진다
.venv/

# 분석 결과물 — 방송 채팅 전문(시청자 닉네임), 영상 절대경로, 미리보기 클립이 들어있다.
# 용량도 수 GB까지 커지므로 절대 커밋하지 않는다.
data/

# 배포용 zip
*.zip

# 캐시 / OS
__pycache__/
*.pyc
.DS_Store

# 로컬 도구 설정
.claude/settings.local.json
