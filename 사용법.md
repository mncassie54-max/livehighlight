# livehl 사용법

방송 하나를 편집하는 전체 과정입니다. 처음이면 위에서부터 그대로 따라가면 됩니다.

---

## 0. 방송 전 — OBS 설정 (한 번만)

이 두 가지를 해두면 하이라이트 정확도가 확 올라갑니다.

**① 오디오 트랙 2개로 녹화하기**

`설정` → `출력` → 위쪽 `고급` 모드로 전환 → `녹화` 탭

- `오디오 트랙`에서 **1번과 2번을 둘 다 체크**

그리고 `오디오 믹서` 패널에서 각 소스의 톱니바퀴 → `오디오 속성 고급`:

| 소스 | 트랙 1 | 트랙 2 |
|---|---|---|
| 마이크 | ✅ | ✅ |
| 데스크탑 음성(게임·BGM) | ✅ | ❌ |

→ 트랙 1 = 전체 소리(클립에 쓸 소리), 트랙 2 = **내 목소리만**

트랙 2가 있으면 게임 소리에 묻히지 않고 내 웃음·리액션을 정확히 잡습니다.
없어도 동작하지만 정확도가 떨어집니다.

**② 파일명 형식은 기본값 그대로 두기**

`설정` → `고급` → `녹화` → `파일명 형식`이 `%CCYY-%MM-%DD %hh-%mm-%ss` 이면 됩니다.
파일명에 박힌 녹화 시작 시각으로 채팅과 영상의 시간축을 맞춥니다.

---

## 1. 툴 켜기

터미널을 열고:

```bash
cd ~/Downloads/youtube-live && ./start.sh
```

브라우저가 자동으로 열립니다. 안 열리면 주소창에 `http://127.0.0.1:8777` 직접 입력.

> 끌 때는 터미널에서 `Ctrl + C`. 켜둔 채로 놔둬도 아무 문제 없습니다.

우측 상단 배지에 `ffmpeg`, `yt-dlp`가 초록색이면 준비 완료입니다.

---

## 2. 프로젝트 만들기 (방송 1회 = 프로젝트 1개)

왼쪽 **`+ 새 방송 분석`** 클릭.

1. **영상 파일 고르기** — 폴더를 눌러 들어가고, 🎬 영상 파일을 클릭하면 아래 "선택된 파일"에 경로가 들어갑니다
   - 상단 `홈` `다운로드` `동영상` `데스크탑` 버튼으로 빠르게 이동
   - 경로를 아는 경우 입력창에 직접 붙여넣고 `이동`
2. **다시보기 URL** 붙여넣기 — 채팅 반응을 쓰려면 필요합니다. 비워도 마이크 신호만으로 동작합니다
   - **유튜브 다시보기 URL** — `https://www.youtube.com/watch?v=...`
   - **치지직 다시보기 URL** — `https://chzzk.naver.com/video/...`
   - ⚠️ **동시송출을 했다면 둘 다 넣으세요.** 한쪽만 넣으면 그 플랫폼 채팅만 보게 되어,
     반대쪽에서만 터진 구간을 통째로 놓칩니다
   - 실제로 재본 방송에서는 **채팅의 93%가 치지직**에 있었습니다(유튜브 201개 / 치지직 2,753개).
     유튜브만 넣었으면 반응의 7%만 보고 하이라이트를 고르는 셈이었습니다
   - 두 다시보기의 시작 시각 차이는 **자동으로 맞춥니다**. 손댈 필요 없습니다
3. **`프로젝트 만들기`**

> 왜 브라우저 기본 파일 선택창이 아닌가: 브라우저는 보안상 파일의 전체 경로를 알려주지 않습니다.
> 5시간짜리 영상을 업로드할 수도 없으니, 서버가 직접 폴더를 읽는 방식으로 만들었습니다.

---

## 3. 분석 돌리기

**`2. 신호 분석`** 카드에서:

- **마이크 트랙** — 보통 `트랙 2`가 자동으로 선택됩니다. OBS에서 2번을 마이크 전용으로 설정했다면 그대로 두세요
- **익스포트 오디오 트랙** — 클립·숏츠에 들어갈 소리. 보통 `트랙 1`(전체 믹스)
- 체크박스 3개는 기본값 그대로 두면 됩니다

**`분석 시작`** 누르고 기다립니다.

| 방송 길이 | 예상 시간 |
|---|---|
| 1시간 | 1~3분 |
| 5시간 | 5~15분 |

진행률 바가 실시간으로 올라갑니다. 이 동안 브라우저 탭을 닫아도 분석은 계속됩니다
(다시 열면 진행 상황이 보입니다).

끝나면 아래에 통계가 뜹니다 — `채팅 823개 · ㅋㅋ 209 · 클립 요청 55 · 슈퍼챗 3 · 분당 91.6개 …`

동시송출이면 **어느 플랫폼에서 몇 개가 왔는지**도 같이 나옵니다 (`유튜브 512 · 치지직 311`).
한쪽이 0이면 그 URL이 잘못됐거나 다시보기가 비공개일 수 있습니다.

---

## 4. 시간축 확인 ⚠️ 가장 중요

**`3. 시간축 정렬`** 카드를 봅니다.

채팅 시간(유튜브 다시보기 기준)과 영상 시간(OBS 녹화 기준)은 다릅니다.
방송 시작 전에 녹화를 먼저 켰으면 그만큼 차이가 나기 때문입니다.
툴이 자동으로 맞추지만, **한 번은 눈으로 확인해야** 합니다.

확인 방법:

1. `6. 하이라이트 후보`에서 아무 후보 하나의 **`미리보기 만들기`** 클릭
2. 재생해서 — **실제로 그 구간에서 웃거나 반응이 터지는지** 봅니다
3. 맞으면 끝. 어긋나면 아래처럼 고칩니다

| 증상 | 조치 |
|---|---|
| 반응이 클립 **끝난 뒤에** 나온다 | 오프셋을 **올린다** (+10, +30 …) |
| 반응이 클립 **시작 전에** 이미 지나갔다 | 오프셋을 **내린다** |
| 전혀 엉뚱하다 | `자동 보정` 버튼을 눌러본다 (신뢰도 4 이상이면 믿을 수 있음) |

오프셋 숫자를 바꾸면 후보 전체가 자동으로 다시 계산됩니다.

---

## 5. 후보 다듬기

**`5. 타임라인`** — 방송 전체를 한눈에 봅니다.
- 파란 선 = 융합 점수, 빨간 점선 = 검출 기준선
- 주황 띠 = 선택된 후보 / 회색 띠 = 해제된 후보
- 마우스를 올리면 시각과 점수, **클릭하면 해당 후보로 이동**

**`7. 하이라이트 후보`** — 후보 하나하나 처리합니다.

각 후보에 표시되는 것:
```
☑ #3  ✂️ 클립 요청   1:37:20 ~ 1:38:05 (45초)          점수 7.15
      클립 요청 +5.1, 채팅 폭주 +12.0, 리액션 큼 +12.0     ← 왜 뽑혔는지
      1:37:58 시청자: 이거 클립 ㅋㅋ                      ← 그 순간 채팅
      1:38:01 후원자: 이거 꼭 편집해주세요
```

라벨 뜻:

| 라벨 | 의미 |
|---|---|
| ✂️ 클립 요청 | 시청자가 "클립", "박제", "편집해줘", "명장면"이라고 직접 지목하거나 치지직 **"컷!" 가위 이모티콘**을 씀 — **가장 믿을 만함**. 그냥 "편집"이나 "다시보기" 얘기는 요청이 아니라서 세지 않습니다 |
| 💸 슈퍼챗 | 후원이 몰린 순간 |
| 😂 채팅 폭소 | 채팅창에 ㅋㅋ가 쏟아진 순간. 치지직 **웃음 이모티콘**(ㅋㅋㅋ글자·ㄹㅇㅋㅋ·눈물웃음 등)도 같이 셉니다 |
| 🤣 본인 폭소 | 내가 크게 웃은 순간 |
| 🔥 채팅 흥분 | ㄷㄷ, 대박, 레전드, 헐 … |
| 💬 채팅 폭주 | 이유는 몰라도 채팅이 폭발한 순간 |
| 📢 리액션 큼 | 목소리가 크게 올라간 순간 |

할 일:
- **체크박스** — 쓸 것만 남깁니다 (`전체 선택` / `전체 해제` / `상위 10개만` 버튼 활용)
- **제목/메모 입력** — 프리미어 마커 이름으로 들어갑니다. 나중에 편집할 때 훨씬 편합니다
- **±5초 버튼** — 앞뒤를 다듬습니다. 시작이 늦게 잡혔으면 `◀ 시작-5`
- **미리보기 만들기** — 확실하지 않은 후보만 확인. 전부 만들 필요는 없습니다

---

## 6. 채팅에서 키워드로 찾기

자동 검출은 **"반응이 튀었는지"만** 봅니다. 무엇에 대한 반응이었는지는 모릅니다.
그래서 "그 게임 얘기한 데가 어디였지", "그 사람 이름 나온 데" 같은 건 못 찾습니다.

**`6. 채팅에서 키워드로 찾기`** 는 반대로 갑니다. 채팅 원문에서 낱말을 찾고,
그 낱말이 **몰린 자리**를 구간으로 묶어 줍니다.

```
롤 발로란트          ← 띄어쓰기로 여러 개. 하나라도 있으면 찾습니다
"그 게임"            ← 따옴표로 묶으면 띄어쓰기까지 그대로 찾습니다
```

영어는 대소문자를 가리지 않고, 한국어는 조사가 붙어도 찾습니다(`롤` → `롤을`, `롤이`).

| 설정 | 기본값 | 무슨 뜻인가 |
|---|---|---|
| 묶는 간격 | 30초 | 이보다 멀리 떨어진 채팅은 다른 구간으로 봅니다 |
| 앞 여유 | 15초 | 찾은 자리 앞에 더 붙일 시간. 반응은 사건보다 늦게 오므로 앞을 더 줍니다 |
| 뒤 여유 | 10초 | 찾은 자리 뒤에 더 붙일 시간 |
| 최소 횟수 | 1 | 2 이상으로 두면 **지나가듯 한 번 나온 낱말은 뺍니다** |

결과에는 구간마다 **몇 번 나왔는지 · 유튜브/치지직 어디였는지 · 그때 채팅**이 붙습니다.
쓸 만하면 **`후보에 추가`** 를 누르세요. 후보 목록에 `🔎` 표시로 들어가고,
그다음은 자동으로 찾은 후보와 똑같습니다 — 미리보기, 숏츠, 프리미어 XML 전부 됩니다.

> **다시 검출해도 지워지지 않습니다.** 손으로 찍어둔 자리라서, 임계값을 바꿔
> 다시 검출해도 `🔎` 구간은 그대로 남습니다. 지우려면 체크를 해제하세요.

같은 자리를 이미 후보로 갖고 있으면 건너뜁니다(마커가 두 개 생기지 않게).

---

## 7. 후보 수가 마음에 안 들 때

**`4. 검출 설정`** 슬라이더를 조정하고 **`다시 검출`**을 누릅니다.
재분석이 아니라 캐시된 신호로 다시 계산하므로 **1초면 끝납니다**. 마음껏 시도해보세요.

| 상황 | 조정 |
|---|---|
| 후보가 너무 많다 | `검출 임계값` ↑ (1.8 → 2.2~2.5), `최대 후보 수` ↓ |
| 놓친 게 있다 | `검출 임계값` ↓ (1.4~1.6) |
| 채팅이 적은 방송 | `마이크 음량`·`웃음 진동` 가중치 ↑, 채팅 계열 ↓ |
| 채팅이 아주 활발 | `클립 요청`·`슈퍼챗` 가중치 ↑ |
| 숏츠용으로 짧게 | `앞 여유`/`뒤 여유`를 10/10, `최대 길이`를 60으로 |
| 후보들이 너무 붙어 나온다 | `후보 최소 간격` ↑ (45 → 90) |

---

## 8. 내보내기

**`8. 내보내기`** — 용도에 맞는 버튼을 누릅니다.

### 20분 영상 만들 때

**`프리미어 XML · 러프컷`**

후보 구간만 순서대로 이어붙인 시퀀스가 만들어집니다. 프리미어에서 열어 다듬으면 바로 완성입니다.

### 어디를 쓸지 직접 고르며 편집할 때

**`프리미어 XML · 마커`**

원본 전체가 통째로 놓인 시퀀스 + 후보마다 마커가 찍힙니다.
`창` → `마커` 패널을 열고 마커를 하나씩 옮겨 다니며 컷하면 됩니다.

### 숏츠 만들 때

**`숏츠 일괄 (9:16)`** — 1080×1920 파일이 후보마다 하나씩 나옵니다.

옵션:
- **블러 배경** — 원본 화면을 그대로 두고 위아래를 블러로 채웁니다. 게임 화면이 잘리면 안 될 때
- **꽉 채우기** — 좌우를 잘라 화면을 꽉 채웁니다. `크롭 중심` 슬라이더로 어디를 남길지 지정 (얼굴캠이 오른쪽 아래면 0.7~0.8)
- **숏츠 최대 길이** — 기본 59초. 넘는 후보는 가장 반응이 컸던 지점 중심으로 잘립니다

개별 후보의 **`숏츠로 뽑기`** 버튼으로 하나만 뽑을 수도 있습니다.

### 프리미어로 가져오기

프리미어 → `파일` → `가져오기` → 생성된 `.xml` 선택.

XML과 함께 **CSV·TXT 후보 목록**도 같이 저장됩니다 (타임코드 + 근거 + 그 순간 채팅).
영상 설명란에 타임스탬프 넣을 때 그대로 쓸 수 있습니다.

각 결과 옆 **`Finder에서 보기`**를 누르면 파일 위치가 바로 열립니다.

> **mkv 자동 처리**: 프리미어는 .mkv를 읽지 못합니다. OBS 기본 포맷이 mkv라서,
> 툴이 **재인코딩 없이** mp4로 다시 포장해서 그 경로를 XML에 넣습니다.
> 화질·용량은 원본 그대로이고 5시간 영상도 몇 분이면 끝납니다.
> 프리미어에서는 이 mp4를 소스로 쓰게 됩니다.

---

## 9. 정리

편집이 끝난 방송은 **`프로젝트 삭제`**로 정리합니다.
미리보기 클립과 리먹스된 mp4가 쌓이면 용량이 커집니다.

**원본 영상은 절대 지우지 않습니다.** `data/` 폴더 안에 툴이 만든 것만 삭제됩니다.

---

## 문제 해결

| 증상 | 원인·해결 |
|---|---|
| `포트 8777이 이미 사용 중` | 이미 켜져 있습니다. 브라우저에서 `http://127.0.0.1:8777` 열면 됩니다. 다른 포트로 띄우려면 `./start.sh --port 8888` |
| 채팅 통계가 0개 | 다시보기가 비공개거나 채팅 리플레이가 없는 방송입니다. 마이크 신호만으로도 동작하니 `마이크 음량`·`웃음 진동` 가중치를 올리세요 |
| 후보 위치가 전부 어긋남 | 시간축 오프셋 문제입니다. 위 **4번** 참고 |
| 후보가 하나도 안 나옴 | `검출 임계값`을 1.2까지 내려보세요. 그래도 없으면 마이크 트랙을 잘못 골랐을 수 있습니다 (`분석 시작`을 다시) |
| 웃음을 잘 못 잡음 | 마이크 트랙이 게임 소리가 섞인 믹스 트랙일 가능성이 큽니다. OBS 멀티트랙 설정(위 **0번**) 후 다음 방송부터 개선됩니다 |
| 미리보기 만들기가 오래 걸림 | 정상입니다. 5시간 영상의 뒷부분을 찾아가야 하므로 후보 1개당 10~40초 걸립니다 |
| 브라우저를 닫아버렸다 | 분석은 계속 돌고 있습니다. 다시 `http://127.0.0.1:8777` 열면 됩니다 |
| 환경 배지가 빨간색 | `./start.sh --doctor`로 확인. `pip install -r requirements.txt`를 다시 |

---

## 다음 방송부터 반복하는 흐름

익숙해지면 이 정도입니다:

```
./start.sh
→ + 새 방송 분석 → 파일 + URL → 프로젝트 만들기
→ 분석 시작 (커피 한 잔)
→ 후보 하나 미리보기로 시간축 확인
→ 상위 10개만 → 제목 적기
→ 프리미어 XML · 러프컷  또는  숏츠 일괄
```
