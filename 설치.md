# 다른 Mac 에 설치하기

Intel Mac, Apple Silicon(M1~M4) 둘 다 됩니다. 필요한 것들은 첫 실행 때 각 Mac에 맞는 버전으로
자동 설치되므로, **소스 폴더만 옮기면 됩니다.**

---

## 준비물 확인 (새 Mac에서 1분)

터미널을 열고 (`Command + Space` → "터미널"):

```bash
python3 --version
```

- 버전이 나오면 → 준비 완료. 아래 **옮기는 방법**으로
- `command not found` 또는 개발자 도구 설치 창이 뜨면 → 창의 `설치` 버튼을 누르고 5~10분 기다립니다.
  창이 안 떴으면 직접:

```bash
xcode-select --install
```

> 파이썬 3.9 이상이면 됩니다. macOS 13 이후 버전은 개발자 도구만 깔면 자동으로 들어옵니다.

인터넷 연결도 필요합니다 (첫 실행 때 약 40MB 정도 내려받습니다).

---

## 방법 1 — zip 으로 옮기기 (권장)

### 보내는 Mac 에서

```bash
cd ~/Downloads/youtube-live && ./package.sh
```

데스크탑에 `livehl-20260804.zip`이 생깁니다. **1MB도 안 됩니다** —
가상환경과 분석 데이터는 빼고 소스만 담기 때문입니다.

### 받는 Mac 에서

1. zip 을 **에어드롭 / USB / 카카오톡 / 구글드라이브** 등 아무 방법으로 옮깁니다
2. zip 을 **더블클릭**해서 압축을 풉니다
3. 풀린 폴더를 원하는 곳으로 옮깁니다 (예: `~/Downloads/youtube-live`)
4. 터미널에서:

```bash
cd ~/Downloads/youtube-live && ./start.sh
```

첫 실행은 2~5분 걸립니다 (가상환경 만들고 numpy·yt-dlp·ffmpeg 내려받기).
끝나면 브라우저가 자동으로 열립니다. **두 번째 실행부터는 몇 초면 켜집니다.**

폴더 경로가 다르면 `cd` 뒤를 바꾸면 됩니다. 경로를 모를 때는 터미널에 `cd ` 를 치고
(뒤에 **공백 한 칸**) 폴더를 터미널 창으로 **드래그**하면 경로가 자동 입력됩니다.

---

## 방법 2 — GitHub 로 관리하기

여러 Mac에서 계속 쓰거나 수정 이력을 남기고 싶다면 이 방법이 낫습니다.

### 처음 한 번 (보내는 Mac)

git 을 처음 쓴다면 이름·메일을 먼저 등록합니다 (커밋에 기록될 정보):

```bash
git config --global user.name "최지영"
git config --global user.email "본인메일@example.com"
```

저장소를 만듭니다:

```bash
cd ~/Downloads/youtube-live
git init
printf '.venv/\ndata/\n__pycache__/\n*.pyc\n.DS_Store\n*.zip\n' > .gitignore
git add -A
git commit -m "livehl 초기 커밋"
```

**올리기 전에 확인** — 방송 데이터가 섞여 들어가지 않았는지 봅니다:

```bash
git ls-files | grep -E '^(data|\.venv)/' | head
```

아무것도 안 나오면 정상입니다. 뭔가 나오면 `git rm -r --cached data .venv` 후 다시 커밋하세요.

이제 GitHub 웹에서:

1. [github.com/new](https://github.com/new) 접속
2. `Repository name`: `livehl`
3. **`Private` 선택** ← 공개로 만들면 누구나 볼 수 있습니다
4. `Add a README file` 등은 **체크하지 않음** (이미 있으므로 충돌합니다)
5. `Create repository`

만들어진 페이지에 나오는 `…push an existing repository` 명령을 그대로 복사해 실행합니다.
대략 이런 형태입니다:

```bash
git remote add origin https://github.com/<내계정>/livehl.git
git branch -M main
git push -u origin main
```

비밀번호를 물으면 계정 비밀번호가 아니라 **Personal Access Token** 을 넣어야 합니다.
`GitHub 우측상단 프로필 → Settings → Developer settings → Personal access tokens →
Tokens (classic) → Generate new token` 에서 `repo` 권한으로 만들어 붙여넣으세요.

> `gh` CLI(`brew install gh`)를 쓰면 `gh auth login` 후 `gh repo create livehl --private --source=. --push`
> 한 줄로 끝납니다. 이 Mac에는 아직 설치되어 있지 않습니다.

### 받는 Mac

```bash
cd ~/Downloads
git clone https://github.com/<내계정>/livehl.git youtube-live
cd youtube-live && ./start.sh
```

이후 수정사항 반영은 `git pull` 한 번이면 됩니다.

---

## 방법 3 — iCloud / 드롭박스 폴더에 두기

`~/Library/Mobile Documents/com~apple~CloudDocs/`(iCloud 드라이브) 안에 폴더를 두면
두 Mac에서 자동 동기화됩니다. 간단하지만 주의할 점이 있습니다:

- **`.venv` 와 `data` 폴더는 동기화되면 안 됩니다.** 가상환경은 Mac 아키텍처별로 다르고
  (Intel↔Apple Silicon 호환 안 됨), `data`는 수 GB까지 커집니다
- 동기화 폴더에 두려면 각 Mac에서 `.venv`를 지우고 다시 `./start.sh` 하는 편이 안전합니다

계속 쓸 거라면 **방법 2(GitHub)** 를 권합니다.

---

## 옮기면 안 되는 것

| 폴더 | 이유 |
|---|---|
| `.venv/` | 파이썬 가상환경. Mac 아키텍처(Intel/ARM)와 경로에 묶여 있어 옮기면 깨집니다. 각 Mac에서 새로 만들어야 합니다 |
| `data/` | 분석 결과·미리보기 클립. 수 GB까지 커집니다. 방송 영상 자체가 그 Mac에 없으면 의미도 없습니다 |
| `__pycache__/` | 캐시. 자동 생성됩니다 |

`package.sh`는 이 세 개를 자동으로 제외합니다.

---

## 설치 확인

```bash
./start.sh --doctor
```

이렇게 나오면 정상입니다:

```json
{
  "ffmpeg": "...../imageio_ffmpeg/binaries/ffmpeg-macos-...",
  "ffmpeg_version": "ffmpeg version 7.1 ...",
  "ffprobe": null,
  "ffprobe_note": "ffprobe 없음 → ffmpeg 출력 파싱으로 대체 (정상 동작)",
  "yt_dlp": "2025.10.14",
  "ok": true
}
```

`"ok": true` 면 됩니다. `ffprobe`가 `null`인 건 **정상**입니다 — ffmpeg 출력을 직접 읽는
방식으로 대체하도록 만들어져 있습니다.

Homebrew를 이미 쓰고 있다면 `brew install ffmpeg`를 해두면 그쪽을 우선 사용합니다
(ffprobe도 함께 생겨 미디어 정보를 조금 더 정확하게 읽습니다). **필수는 아닙니다.**

---

## 문제 해결

| 증상 | 해결 |
|---|---|
| `permission denied: ./start.sh` | `chmod +x start.sh package.sh` 실행 후 다시 |
| `command not found: python3` | 위 **준비물 확인** 참고 (`xcode-select --install`) |
| `"확인할 수 없는 개발자"` 류 차단 | 브라우저로 zip을 받으면 격리 표시가 붙습니다. `xattr -dr com.apple.quarantine ~/Downloads/youtube-live` 실행 후 다시 |
| pip 설치가 매우 느리거나 실패 | 회사·학교 네트워크의 프록시일 수 있습니다. 다른 네트워크(휴대폰 핫스팟)로 한 번만 시도 |
| `포트 8777이 이미 사용 중` | 이미 켜져 있습니다. `http://127.0.0.1:8777` 을 열거나 `./start.sh --port 8888` |
| 방화벽 허용 창이 뜬다 | 허용하지 않아도 됩니다. `127.0.0.1`(내 Mac 안)에서만 동작하고 외부에서 접속할 수 없습니다 |
| Intel Mac에서 만든 zip을 M1에서 풀었는데 안 됨 | `.venv`가 섞여 들어간 경우입니다. `rm -rf .venv` 후 `./start.sh` |

---

## 처음부터 다시 깔고 싶을 때

```bash
cd ~/Downloads/youtube-live
rm -rf .venv
./start.sh
```

`data/`는 건드리지 않으므로 기존 분석 결과는 그대로 남습니다.
